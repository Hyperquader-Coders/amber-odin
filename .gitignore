/build/
/out/
/dist/

# Claude config dirs, if a session ever materialises one here.
.claude/
.mcp.json
.claude-amber/
.credentials.json

# Generated by `make release` (write-only; GitHub Releases are the record now).
RELEASE.md

# Agent files are local and are not shared.
.claude/

# Claude config dirs, if a session ever materialises one here.
.claude-amber/
.credentials.json
